---
import NavigationLink from "./NavigationLink.astro";
import  siteConfig  from "./content/data/navigations.json";
const { id } = Astro.props;
type LinkType = {
    url: string,
    label: string,
    description: string,
}
---

<nav class="absolute inset-x-4 lg:static top-full z-2">
  <div
    id={id}
    class="hidden flex-col lg:flex lg:flex-row top-10 lg:gap-10 text-center gap-10 bg-neutral-50 lg:border-none border border-gray-300 rounded-2xl"
  >
    {
      siteConfig.mainMenu.map((link: LinkType) => (
        <NavigationLink
          url={link.url}
          title={link.title}
          description={link.description}
        />
      ))
    }
  </div>
</nav>
