---
import Config from '../data/infoGeneral.json';

const { text, isVisible } = Config.site.message;
---

{
  isVisible && (
    <div class="top-banner text-background bg-foreground w-full py-4 text-center text-lg">
      {text}
    </div>
  )
}
