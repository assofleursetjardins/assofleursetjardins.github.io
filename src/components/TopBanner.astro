---
import Config from '../data/infoGeneral.json';

const { text, isVisible } = Config.site.message;
---

{
  isVisible && (
    <div class="top-banner text-background bg-foreground py-4 text-lg text-center w-full">
      {text}
    </div>
  )
}
