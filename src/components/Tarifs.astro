---
import  siteConfig  from "../data/editonSalon.json";
const { ticketPrice, parkingPrice } = siteConfig.salon.pricing;
---

<div>
    <h2>Tarifs</h2>
    <ul>
        {ticketPrice && <li>{ticketPrice}</li>}
        {parkingPrice && <li>{parkingPrice}</li>}
</div>
