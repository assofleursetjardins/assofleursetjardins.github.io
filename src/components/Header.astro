---
import TopBanner from "./TopBanner.astro";
import Burger from "./ui/Burger.astro";
import Navigation from "./Navigation.astro";
import Logo from "./Logo.astro";
import  siteConfig  from "../data/navigations.json";

const home = siteConfig.navigations.home.url;
---

<TopBanner />
<header
  id="main-header"
  class="flex items-center justify-between px-2 py-4 relative max-w-7xl mx-auto"
>
  <a class="block w-[200px] lg:w-[300px]" href={home}>
    <Logo />
  </a>
  <Navigation id="main-nav-container" />
  <Burger id="main-burger-menu" />

  <script>
    const burgerMenu = document.querySelector("#main-burger-menu");
    const navContainer = document.querySelector("#main-nav-container");

    if (burgerMenu && navContainer) {
      burgerMenu.addEventListener("click", () => {
        navContainer.classList.toggle("flex");
        navContainer.classList.toggle("hidden");
      });
    }
  </script>
</header>
