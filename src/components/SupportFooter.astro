---
import { Image } from "astro:assets";
import "../styles/global.css";

type LogoPartenairesType = {
    path: string
    alt: string
}

const LogoPartenaires: LogoPartenairesType[] = [
    {path:'/images/logo-le-vaudreuil.jpg',
     alt:"logo-le-vaudreuil"},

    {path:'/images/logo-seine-eure.jpg',
    alt:"logo-le-vaudreuil"},

    {path:'/images/logo-francebleu-normandie.jpg',
    alt:"logo-le-vaudreuil"},
]

---
<div class="px-3 mb-11 box-border w-full">
    <h4 class="text-foreground-contrast font-bold my-4">Avec le soutien de</h4>
    <div class="flex flex-wrap justify-left">
        {LogoPartenaires.map((item: LogoPartenairesType) => (
            <Image class="filter grayscale m-3.5" src={item.path} width={120} height={120} alt={item.alt} />
        ))}
    </div>
</div>