---
import Layout from "../layouts/Layout.astro";
const {
    metaTitle = "",
    metaDescription = "",
    pageTitle = "À propos",
    pageDescription = "",
} = Astro.props;
---

<Layout title={metaTitle} />
<header>
    <h1>{pageTitle}</h1>
    {pageDescription && <p>{pageDescription}</p>}
</header>
